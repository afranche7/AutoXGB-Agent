[project]
name = "${BUNDLE_NAME}"
version = "0.1.0"
description = "AutoXGB inference bundle: predicts '${TARGET}' (${TASK_TYPE}), run ${RUN_ID}."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.0",
    "pyarrow>=14",
    "xgboost>=2.1",
]

[tool.uv]
package = false
